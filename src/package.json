{
  "name": "Dragon Story",
  "version": "0.2",
  "description": "An online game where you raise a dragon from egg to adulthood.",
  "keywords": ["Dragon Story game"],
  "license": "proprietary",
  "author": "John Newell",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
